function myFunction() {
    var sentence= "I am learning ";
    sentence += " a lot from this book!"; //using the operater to concatenate a string
    document.getElementById("Concatenate").innerHTML= sentence; //ID name referencing 'Concatenate' given in html file
}